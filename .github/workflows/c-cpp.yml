name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build_run:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: build & run
      run: ./build_and_run_all_days.sh
